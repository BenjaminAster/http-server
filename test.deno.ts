
// import { setCwd } from "https://deno.land/x/chdir_anywhere@v0.0.2/mod.js";
// setCwd();

// const p = Deno.run({
//   cmd: ["deno", "types", "--unstable"],
//   stdout: "piped",
// });

// const typesContent = await p.output();
// await Deno.writeFile("./test.d.ts", typesContent);

/// <reference types="deno-types" />

Deno.serve
